models:
  - name: inventory
    columns:
      - name: inventory_id
        tests:
        - unique
        - not_null
      - name: film_id
        tests:
        - not_null
        - relationships:
            to: ref('film')
            field: film_id